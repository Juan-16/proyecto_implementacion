server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Necesario para React Router: cualquier ruta que no sea un archivo
    # real cae en index.html y React Router decide qué mostrar.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
